{
  "name": "roadcopilot",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=@roadcopilot/mobile",
    "build:contracts": "npm run build --workspace=@roadcopilot/contracts"
  },
  "engines": {
    "node": ">=18"
  }
}
